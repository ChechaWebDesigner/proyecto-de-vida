export function writeEffect(selector) {
  const typed = new Typed(selector, {
    strings: [" César González"],
    typeSpeed: 100,
    startDelay: 500,
    backSpeed: 100,
    smartBackspace: true,
    backDelay: 1000,
    loop: true,
    loopCount: false,
    showCursor: true,
    contentType: "html",
  });
}
